import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  // register state'inin initial state'i
};

const registerSlice = createSlice({
  name: "register",
  initialState,
  reducers: {
    // register reducer'inin reducer'leri
  },
});

export default registerSlice.reducer;